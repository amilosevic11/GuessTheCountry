<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amilosevic.guessthecountry">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".GuessTheCountryApp"
        android:allowBackup="true"
        android:icon="@drawable/guessthecountrylogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/guessthecountrylogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuessTheCountry">
        <activity android:name=".activities.PlayQuizActivity"></activity>
        <activity android:name=".activities.PlayOrSeeResultsActivity" />
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>